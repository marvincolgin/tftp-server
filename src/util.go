package main

import "os"

// fileExists determines if the fileexists and it's *NOT* a directory
func fileExists(filename string) bool {
	info, err := os.Stat(filename)
	if os.IsNotExist(err) {
		return false
	}
	return !info.IsDir()
}
